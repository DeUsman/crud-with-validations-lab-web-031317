<!DOCTYPE html>
<html>
<table>
<thead>
	<th>Title</th>
	<th>Artist</th>
	<th>Genre</th>
	<th>Release Year</th>
</thead>
<tbody>
<h1>List of all Your Songs</h1>
	<% @songs.each.with_index(1) do |song, index| %>
	<tr>
		<td><%= link_to "#{index} #{song.title}", song_path(song) %></td>
		<td><%= song.artist_name%>   </td>
		<td><%= song.genre %></td>
		<td><%= song.release_year %><br></td>
	<% end %>

</tbody>
</table>
</html>