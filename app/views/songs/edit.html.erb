<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
<% Song.all.each.with_index(1) do |song, index| %>
<div class="songs">
		<%= link_to "Delete", song, method: :delete, data: { confirm: "Really" } %><br>
		</div>
<% end %>

	<h1>Here you can edit only the song that you have selected</h1>
	<h3>Song to Edit: <%= @song.title %></h3>
<%= form_for( @song ) do |f| %>
  <%= f.label :title %><br>
  <%= f.text_field :title %><br>
  <%= f.label :released %>
  <%= f.check_box :released %><br>
  <%= f.label :release_year %><br>
  <%= f.number_field :release_year %><br>
  <%= f.label :artist_name %><br>
  <%= f.text_field :artist_name %><br>
  <%= f.label :genre %><br>
  <%= f.text_field :genre %>
  <%= f.submit %>
<% end %>

</body>
</html>